# Electron App Environment Variables
# App Configuration
THEME=
# Options: "windows" | "osx" | "linux" | "" (empty = auto-detect from OS)
# Leave empty to automatically detect the operating system theme
